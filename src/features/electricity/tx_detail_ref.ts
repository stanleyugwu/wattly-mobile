import { TxDetailRef } from "./types";

/**
 * This will facilitate passing tx data from e.g electricity top-up screen
 * to tx detail screen for viewing and sharing.
 */
export const txDetailRef: TxDetailRef = {
  details: null,
};
